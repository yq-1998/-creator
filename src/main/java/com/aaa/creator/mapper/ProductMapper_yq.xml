<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aaa.creator.mapper.ProductMapper_yq">
    <select id="selproduct" resultType="Product" >
        select * from product where sid=#{sid} and pstate=0
    </select>
    <select id="selPro" resultType="Product">
        select * from product where pstate=0
    </select>
</mapper>

